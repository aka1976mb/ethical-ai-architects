# Ethical AI Audit CLI

A command-line interface tool to generate Model Cards, Data Statements, and Risk Registers from a configuration file.

## Usage

1.  Place your `audit.config.json` in the root of this directory or specify its path as an argument.
2.  Run `npm start`.

This will generate an `audit.md` file with the documented information.
